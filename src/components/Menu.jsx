import { observer } from 'mobx-react-lite'
import React from 'react'
import {store} from '../store'

const Menu = observer(() => {
    return (
        <div>
            menu
        </div>
    )
})

export default Menu
